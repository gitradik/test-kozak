const {
    createWorker,
    getWorkerById,
    getWorkers,
    updateWorker,
} = require('./workerController');

const controllers = {
    createWorker,
    getWorkerById,
    getWorkers,
    updateWorker,
};

module.exports = controllers;