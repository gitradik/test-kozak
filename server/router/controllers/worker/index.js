const {
    createWorker,
    getWorkerById,
    getWorkers,
} = require('./workerController');

const controllers = {
    createWorker,
    getWorkerById,
    getWorkers,
};

module.exports = controllers;